import * as Phaser from 'phaser';

export class Game extends Phaser.Scene {
  // propetry

  constructor() {
    super('Game');
  }

  preload() {
  }

  create() {
  }

  update(time: number, delta: number) {
  }

  end() {
  }
}
